<link href="~/css/bootstrap.min.css" rel="stylesheet" />

<style>
    .main {
          min-height: 100vh;
          display: flex;
          align-items: center;
    }

    .main-card {
          background: rgba(0, 0, 0, 0.65);
          backdrop-filter: blur(3px);
          border-radius: 20px;
          padding: 3rem;
          color: #fff;
          max-width: 550px;
    }

    .main-card h1 {
        font-size: 3rem;
        font-weight: 700;
    }

    .main-img {
          position:relative;
          height: 135vh;
          object-fit: cover;
          width: 100%;
    }

    .btn-outline-light {
          border-radius: 50px;
          padding: 0.75rem 2rem;
          letter-spacing: 2px;
    }
     
    .card {
        background: rgba(255, 255, 255, 1);
        backdrop-filter: blur(3px);
        border-radius: 20px;
        padding: 3rem;
        color: #000;
        max-width: 280px;
    }
    .card h2 {
        font-size: 3rem;
        font-weight: 700;
    }
    

    .hero-section {
        background: url('../../wwwroot/img/1.jpg') center/cover no-repeat;
        min-height: 100vh;
        position: relative;
        display: flex;
        align-items: center;
    }

    .overlay {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.4);
    }

    .content-wrapper {
        position: relative;
        z-index: 2;
    }

    .menu-card {
        background: rgba(0, 0, 0, 0.6);
        border-radius: 20px;
        padding: 30px 15px;
        text-align: center;
        color: #fff;
        transition: 0.3s;
        height: 100%;
    }

    .menu-card:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-5px);
    }

    .menu-card i {
        font-size: 40px;
        margin-bottom: 15px;
    }

    .credit {
        color: #ccc;
        font-size: 14px;
        margin-top: 20px;
    }

    .credit a {
        color: #fff;
        text-decoration: underline;
    }

    .contact-section {
        min-height: 100vh;
        display: flex;
        align-items: center;
    }

    .contact-img {
        background: url('your-image.jpg') center center / cover no-repeat;
        min-height: 600px;
    }

    .form-control,
    .form-control:focus {
        border: none;
        border-bottom: 2px solid #000;
        border-radius: 0;
        box-shadow: none;
        background-color: transparent;
    }

    textarea.form-control {
        resize: none;
    }

    .btn-dark-custom {
        background: #000;
        color: #fff;
        border-radius: 30px;
        padding: 12px 30px;
        font-weight: 600;
        letter-spacing: 1px;
    }

        .btn-dark-custom:hover {
            background: #333;
            color: #fff;
        }

    .footer-dark {
        background: #2b2b2b;
        color: #ccc;
        padding: 60px 0;
        text-align: center;
    }

        .footer-dark a {
            color: #9ecbff;
        }

</style>